class Load:
    def __init__(self, loadMagnitude: float):
        self.loadMagnitude = loadMagnitude

    def calcFixedEndMoments(self, length) -> list[float]:
        return

    def calcMomentByLoad(self, length) -> float:
        return

    def calcForceByLoad(self, length) -> float:
        return
